# Screenshots for `blog/2026/07/recognition-intelligence`

Drop the real screenshots here using these exact filenames and they replace the dashed
placeholders automatically — no edits to the `.mdx` needed.

| File | Shot |
|---|---|
| `app-home.png` | Sample app home / tab bar |
| `face-recognize.png` | Live recognition — overlay box + matched name |
| `face-enroll.png` | Guided enrollment — target oval overlay |
| `voice-enroll.png` | Voice wizard — prompt sentence + agreement score |
| `voice-enroll-control.png` | `VoiceEnrollmentView` mid-run — prompt list ticked off, countdown, VU meter with the threshold tick |
| `voice-recognize.png` | Voice ID result + nearest distance |
| `face-people.png` | People list — thumbnails + shot counts |
| `voice-speakers.png` | Speakers list — recording counts |
| `document-scan.png` | Extracted receipt / document fields |

Phone screenshots render at a 9:19.5 slot (max 320px wide, or 240px when paired in a row).
Any web format works — if you use something other than `.png`, update the `src` in
`src/content/blog/2026/07/recognition-intelligence.mdx` to match.
